# midPoint 4.9.2 Cluster
3 nodes + NGINX + SSL

## Запуск:
1. Скачайте midpoint-4.9.2-dist.tar.gz ? распакуйте в `./midpoint-4.9.2/`
2. Сгенерируйте SSL в `./ssl/`
3. `cp .env.example .env`
4. `docker compose up -d`